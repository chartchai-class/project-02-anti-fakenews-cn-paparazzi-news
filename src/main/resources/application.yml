spring:
  datasource:
    url: jdbc:mysql://localhost:3306/antifakenews?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: antifake
    password: antifakepwd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
server:
  port: 8080

app:
  jwt:
    secret: change-this-secret-in-prod-please-keep-safe
    expiration-seconds: 2592000
  uploads:
    dir: uploads
